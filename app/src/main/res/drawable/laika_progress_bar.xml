<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <solid android:color="@color/laika_red" />
            </shape>
        </clip>

        <color android:color="@color/laika_red" />
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <solid android:color="@color/picture_background" />
            </shape>
        </clip>

        <color android:color="@color/picture_background" />
    </item>

</layer-list>