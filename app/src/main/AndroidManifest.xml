<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.laikachile.laika" >

    <!-- [AI] [G+] So that we can get the account name -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <!-- [AI] [G+] Used to request data from the Google+ APIs in the SignInActivity -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--  [AI] [G+] Required if we need to invalidate access tokens -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS"/>

    <!--  [AI] [CAM] Requerido para escribir sobre la tarjeta sd -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <uses-feature android:name="android.hardware.camera"
        android:required="true" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true"/>

    <application
        android:name="com.activeandroid.app.Application"
        android:allowBackup="true"
        android:icon="@drawable/laika_logo"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".activities.WelcomeActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.MainActivity"
            android:label="@string/main_activity_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.GPlusSignInActivity"
            android:label="@string/log_in_activity_name"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.MapActivity"
            android:label="Veterinarias" >
        </activity>

        <activity
            android:name="cl.laikachile.laika.activities.MapHairActivity"
            android:label="Peliquerías" >
        </activity>

        <!-- Activities de Laika -->

        <activity
            android:name="cl.laikachile.laika.activities.AdoptDogFormActivity"
            android:exported="false"
            android:label="@string/adopt_activity_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>

        <activity
            android:name="cl.laikachile.laika.activities.AdoptDogFragmentActivity"
            android:label="@string/adopt_activity_name"
            android:exported="false"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>

        <activity
            android:name="cl.laikachile.laika.activities.AdoptDogSuccessActivity"
            android:label="@string/adopt_activity_name"
            android:exported="false"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>

        <activity
            android:name="cl.laikachile.laika.activities.NewDogRegisterActivity"
            android:exported="false"
            android:label="@string/new_dog_register_activity_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>

        <activity
            android:name="cl.laikachile.laika.activities.MyDogsFragmentActivity"
            android:label="@string/my_dog_activity_name"
            android:exported="false"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>

        <activity
            android:name=".activities.HealthDogActivity"
            android:label="@string/my_dog_health_activity_name"
            android:exported="false"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>

        <activity
            android:name="cl.laikachile.laika.activities.NewsFragmentActivity"
            android:label="@string/news_activity_name"
            android:exported="false"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>

        <activity
            android:name="cl.laikachile.laika.activities.TipsActivity"
            android:label="@string/tips_activity_name"
            android:exported="false"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>

        <activity
            android:name="cl.laikachile.laika.activities.EventsActivity"
            android:label="@string/events_activity_name"
            android:exported="false"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>

        <activity
            android:name="cl.laikachile.laika.activities.MyDogOwnersActivity"
            android:label="@string/my_dog_owners_activity_name"
            android:exported="false"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>

        <activity
            android:name="cl.laikachile.laika.activities.MyDogRemindersActivity"
            android:label="@string/my_dog_reminders_activity_name"
            android:exported="false"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>

        <activity
            android:name="cl.laikachile.laika.activities.GiveInAdoptionActivity"
            android:label="@string/give_in_adoption_activity_name"
            android:exported="false"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>

        <activity
            android:name="cl.laikachile.laika.activities.StoriesFragmentActivity"
            android:label="@string/stories_fragment_activity_name"
            android:exported="false"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>


        <!-- [AI] agregar la metadata de la base de datos -->
        <meta-data android:name="AA_DB_NAME" android:value="AndroidInit.db" />
        <meta-data android:name="AA_DB_VERSION" android:value="5" />

        <!-- [AI] [G+] agregar la metadata de la base de datos -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyCP25kj5jYDw_8Sy7gYijj3CnMliaAYKpw"/>
    </application>

</manifest>
